import sys

if __name__ == "__main__":
    if len(sys.argv)!= 2:
        print("Usage: " + sys.argv[0] + " <string>")
        sys.exit(1)
    else:
        String text = scanner.nextLine();
        int count = 0;
                count++;
        scanner.close();