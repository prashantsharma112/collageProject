def is_palindrome():
    original = input("Enter a word or phrase: ")
    reversed_string = original[::-1]
    if original.lower() == reversed_string.lower():
    else:

if __name__ == "__main__":
    is_palindrome()